# Local sandbox ONLY — never point this at OneDrive or production.
# Loaded only by npm run start:practice / seed:practice / reset:practice.
# .env = real/production path (OneDrive). .env.practice = this sandbox.
# Practice and production must NEVER share a DATA_DIR.
PRACTICE_MODE=true
DATA_DIR=./practice-data
PORT=3847
